<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="threegroup.tl.com.eddemoone">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/TranslucentTheme">
        <meta-data
            android:name="design_width"
            android:value="720">
        </meta-data>
        <meta-data
            android:name="design_height"
            android:value="1280">
        </meta-data>

        <activity android:name=".home.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".mypage.activity.MyMessageActivity">
        </activity>
        <activity android:name=".mypage.activity.SiteActivity">
        </activity>
        <activity android:name=".mypage.activity.NewaddressActivity">
        </activity>
        <activity android:name=".mypage.activity.AboutMeActivity">
        </activity>
        <activity android:name=".mypage.activity.SettingActivity">
        </activity>
        <activity android:name=".mypage.activity.MyCollectActivity">
        </activity>
        <activity android:name=".mypage.activity.QueueActivity">
        </activity>
        <activity android:name=".mypage.activity.FeedbackActivity">
        </activity>
    </application>

</manifest>